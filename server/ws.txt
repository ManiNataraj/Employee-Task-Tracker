Your task is to build a small full-stack SaaS-style dashboard called **“Employee Task Tracker”** using:

* Frontend → React.js / Next.js
* Backend → Node.js + Express
* Database → Any lightweight DB
* Deploy → Vercel
* Features → Login, CRUD Tasks, Filters, Responsive UI 

I’ll give you the easiest and most interview-friendly approach.

---

# Best Stack for Fast Completion

## Frontend

* React + Vite
* Tailwind CSS
* Axios
* React Router DOM

## Backend

* Node.js
* Express.js
* MongoDB Atlas (free cloud DB)

This is beginner-friendly and easy to deploy.

---

# Final Project Structure

```txt
employee-task-tracker/
│
├── client/          # React Frontend
│
├── server/          # Node Backend
│
└── README.md
```

---

# STEP 1 — Create Frontend

## Create React App

Open terminal:

```bash
mkdir employee-task-tracker
cd employee-task-tracker

npm create vite@latest client -- --template react
```

Go inside:

```bash
cd client
```

Install packages:

```bash
npm install
npm install react-router-dom axios
npm install -D tailwindcss @tailwindcss/vite
```

---

# STEP 2 — Setup Tailwind CSS

In `vite.config.js`

```js
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'
import tailwindcss from '@tailwindcss/vite'

export default defineConfig({
  plugins: [react(), tailwindcss()],
})
```

---

In `src/index.css`

```css
@import "tailwindcss";
```

---

# STEP 3 — Create Backend

Go back:

```bash
cd ..
mkdir server
cd server
```

Initialize:

```bash
npm init -y
```

Install packages:

```bash
npm install express mongoose cors dotenv nodemon
```

Create files:

```txt
server/
│
├── server.js
├── .env
├── models/
│   └── Task.js
├── routes/
│   └── taskRoutes.js
```

---

# STEP 4 — Setup MongoDB Atlas

Create free account:

[MongoDB Atlas](https://www.mongodb.com/cloud/atlas?utm_source=chatgpt.com)

Create cluster.

Copy connection string.

Example:

```env
MONGO_URI=your_mongodb_connection
PORT=5000
```

Save in `.env`

---

# STEP 5 — Backend Code

## `models/Task.js`

```js
const mongoose = require("mongoose");

const taskSchema = new mongoose.Schema({
  taskName: String,
  priority: String,
  dueDate: String,
  status: {
    type: String,
    default: "Pending",
  },
});

module.exports = mongoose.model("Task", taskSchema);
```

---

# STEP 6 — Task Routes

## `routes/taskRoutes.js`

```js
const express = require("express");
const router = express.Router();
const Task = require("../models/Task");

router.get("/", async (req, res) => {
  const tasks = await Task.find();
  res.json(tasks);
});

router.post("/", async (req, res) => {
  const task = await Task.create(req.body);
  res.json(task);
});

router.put("/:id", async (req, res) => {
  const updated = await Task.findByIdAndUpdate(
    req.params.id,
    req.body,
    { new: true }
  );

  res.json(updated);
});

router.delete("/:id", async (req, res) => {
  await Task.findByIdAndDelete(req.params.id);
  res.json({ message: "Deleted" });
});

module.exports = router;
```

---

# STEP 7 — Server Setup

## `server.js`

```js
const express = require("express");
const mongoose = require("mongoose");
const cors = require("cors");
require("dotenv").config();

const app = express();

app.use(cors());
app.use(express.json());

mongoose.connect(process.env.MONGO_URI)
.then(() => console.log("MongoDB Connected"))
.catch((err) => console.log(err));

app.use("/api/tasks", require("./routes/taskRoutes"));

app.listen(process.env.PORT, () => {
  console.log("Server running");
});
```

---

# STEP 8 — Run Backend

Add in `package.json`

```json
"scripts": {
  "start": "nodemon server.js"
}
```

Run:

```bash
npm start
```

---

# STEP 9 — Frontend Pages

Inside `client/src`

Create:

```txt
pages/
│
├── Login.jsx
├── Dashboard.jsx
```

Create:

```txt
components/
│
├── TaskForm.jsx
├── TaskList.jsx
├── Filter.jsx
```

---

# STEP 10 — Dummy Login

## `Login.jsx`

```js
import { useState } from "react";
import { useNavigate } from "react-router-dom";

export default function Login() {
  const [email, setEmail] = useState("");
  const [password, setPassword] = useState("");

  const navigate = useNavigate();

  const handleLogin = () => {
    if(email && password){
      localStorage.setItem("user", email);
      navigate("/dashboard");
    }
  };

  return (
    <div className="h-screen flex items-center justify-center">
      <div className="p-6 shadow-lg rounded bg-white">
        <h1 className="text-2xl mb-4">Login</h1>

        <input
          className="border p-2 w-full mb-3"
          placeholder="Email"
          onChange={(e)=>setEmail(e.target.value)}
        />

        <input
          type="password"
          className="border p-2 w-full mb-3"
          placeholder="Password"
          onChange={(e)=>setPassword(e.target.value)}
        />

        <button
          onClick={handleLogin}
          className="bg-blue-500 text-white px-4 py-2 w-full"
        >
          Login
        </button>
      </div>
    </div>
  );
}
```

---

# STEP 11 — Dashboard Logic

## Features

You must include:

✅ Add Task
✅ Edit Task
✅ Delete Task
✅ Complete Task
✅ Filters
✅ Responsive Design

---

# STEP 12 — Axios Setup

Install:

```bash
npm install axios
```

Example fetch:

```js
const res = await axios.get("http://localhost:5000/api/tasks");
```

---

# STEP 13 — Task Create Example

```js
await axios.post("http://localhost:5000/api/tasks", {
  taskName,
  priority,
  dueDate,
  status: "Pending",
});
```

---

# STEP 14 — Mark Completed

```js
await axios.put(`http://localhost:5000/api/tasks/${id}`, {
  status: "Completed",
});
```

---

# STEP 15 — Filter Logic

```js
const filteredTasks = tasks.filter((task) => {
  if(filter === "Completed")
    return task.status === "Completed";

  if(filter === "Pending")
    return task.status === "Pending";

  return true;
});
```

---

# STEP 16 — Responsive UI

Use Tailwind classes:

```html
<div className="grid md:grid-cols-2 lg:grid-cols-3 gap-4">
```

Mobile automatically works.

---

# STEP 17 — Routing

## `App.jsx`

```js
import { BrowserRouter, Routes, Route } from "react-router-dom";
import Login from "./pages/Login";
import Dashboard from "./pages/Dashboard";

function App() {
  return (
    <BrowserRouter>
      <Routes>
        <Route path="/" element={<Login />} />
        <Route path="/dashboard" element={<Dashboard />} />
      </Routes>
    </BrowserRouter>
  );
}

export default App;
```

---

# STEP 18 — Deploy Backend

Use:

[Render](https://render.com?utm_source=chatgpt.com)

Steps:

1. Push backend to GitHub
2. Create Web Service
3. Add environment variables
4. Deploy

You’ll get:

```txt
https://your-api.onrender.com
```

---

# STEP 19 — Deploy Frontend

Use:

[Vercel](https://vercel.com?utm_source=chatgpt.com)

Steps:

1. Push frontend to GitHub
2. Import project in Vercel
3. Add backend API URL
4. Deploy

---

# STEP 20 — Important Final Changes

Replace:

```js
http://localhost:5000
```

with:

```js
https://your-render-api-url.onrender.com
```

---

# STEP 21 — README.md

Include:

```md
# Employee Task Tracker

## Tech Stack
- React
- Tailwind CSS
- Node.js
- Express
- MongoDB

## Features
- Login
- Add/Edit/Delete Tasks
- Task Filters
- Responsive UI

## Setup
Frontend:
npm install
npm run dev

Backend:
npm install
npm start
```

---

# STEP 22 — GitHub Push

```bash
git init
git add .
git commit -m "Initial commit"
```

Create repo on:

[GitHub](https://github.com?utm_source=chatgpt.com)

Push:

```bash
git remote add origin YOUR_REPO_URL

git push -u origin main
```

---

# Important Tips For Scrutiny Selection

## Add These Extra Features (Huge Advantage)

### 1. Loading Spinner

```js
Loading...
```

### 2. Toast Messages

Install:

```bash
npm install react-hot-toast
```

### 3. Empty State

```txt
No tasks found
```

### 4. Better UI Cards

Use shadows + rounded corners.

---

# Suggested Timeline

## Day 1

* Backend setup
* MongoDB
* CRUD APIs

## Day 2

* Frontend UI
* Dashboard
* Filters

## Day 3

* Deployment
* README
* Testing

---

# Recommended Final UI

Use:

* Dark navbar
* White task cards
* Priority badges
* Green completed status
* Mobile responsive grid

---

# Minimum APIs You Need

| Method | Endpoint       |
| ------ | -------------- |
| GET    | /api/tasks     |
| POST   | /api/tasks     |
| PUT    | /api/tasks/:id |
| DELETE | /api/tasks/:id |

---

# Final Submission

You must submit:

1. GitHub Repo
2. Live Vercel URL
3. README.md 
